jobs:
  - language: python
    python:
      - "3.9.1"
    services:
      - docker
    before_script: 
      - pip install docker-compose
    script:
      - docker-compose run backend sh -c "python manage.py test"
      - docker-compose run frontend sh -c "quasar test --unit jest"
