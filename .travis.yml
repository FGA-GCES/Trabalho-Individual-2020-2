jobs:
  - language: python
    python:
      - "3.9"
    services:
      - docker
    before_script: 
      - pip install docker-compose
    script:
      - docker-compose run --rm backend sh -c "python manage.py test"
      - docker-compose run --rm frontend sh -c "quasar test --unit jest"
